{
  "name": "carinfo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm --prefix client ci --include=dev && npm --prefix server ci --omit=dev && npm --prefix client run build",
    "start": "node server/src/server.js"
  }
}